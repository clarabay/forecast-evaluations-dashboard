# Capped below 1.54: the tab CSS in app.py targets [data-baseweb="tab"], and
# 1.61 swapped BaseWeb tabs for react-aria ([data-testid="stTab"]), which leaves
# every tab unstyled. Lift this once those selectors are updated.
streamlit>=1.53.1,<1.54
# pandas 1.5.x ships no wheels above cp311 and numpy 1.x none above cp312, so
# capping either one makes the install fail from source on the Python versions
# hosts now default to. Both upper bounds lifted: numpy is used in eight places
# in scoring.py, none of them APIs 2.0 removed.
pandas>=2.0,<3
numpy>=1.26,<3
pyarrow>=14.0
plotly>=5.20
requests>=2.31
sodapy>=2.1.0
epiweeks>=2.1
